{{/* Carries files/initdb/01-create-qa-platform-databases.sh, read at render
     time with .Files.Get. The file under files/ IS the script -- there is no
     second copy to keep in sync; edit it there. */}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: qa-platform-postgres-initdb
  labels:
    app.kubernetes.io/name: qa-platform
    app.kubernetes.io/component: postgres
data:
  01-create-qa-platform-databases.sh: |
{{ .Files.Get "files/initdb/01-create-qa-platform-databases.sh" | indent 4 }}
